package Classwork15Practice;

public interface Heap {
    void add(int value);    //adds new value
    int remove();           // returns the highest priority value in the heap
    int size();             //returns size
}
